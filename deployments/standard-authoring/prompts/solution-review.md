# Solution and test review

Review the frozen solution, test, and test-analysis artifacts for consistency
with the approved task. Approve only when the reference solution is
independent, tests cover the claimed behavior and meaningful edge cases, and
the acceptance surface does not rely on hidden infrastructure. Record one
durable decision using the locked review schema.
