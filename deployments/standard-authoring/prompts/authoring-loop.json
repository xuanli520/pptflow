{"id":"standard-authoring.v3.authoring-loop","version":"3.0.4","turn_prompts":["Read validation_environment_contract before writing files. In the authoring workspace, source/ is a read-only reference and task/ is the only writable candidate directory; write exactly instruction.md, task.toml, environment/Dockerfile, solution/solve.sh, tests/test.sh, and tests_analysis.json under task/. Only provision Debian packages in environment/Dockerfile when the frozen task genuinely requires them. Whenever apt-get is used, switch the default Debian APT sources from HTTP to HTTPS before apt-get update exactly with this guarded command. Bookworm images may ship either /etc/apt/sources.list or deb822 /etc/apt/sources.list.d/*.sources; a missing file must never fail the build:\nRUN if [ -f /etc/apt/sources.list ]; then sed -i 's|^deb http://|deb https://|; s|^deb-src http://|deb-src https://|' /etc/apt/sources.list; fi; for f in /etc/apt/sources.list.d/debian.sources; do if [ -f \"$f\" ]; then sed -i 's|^URIs: http://|URIs: https://|' \"$f\"; fi; done\nNever use sed alternation or \\1 backreference expressions for this switch; the controlled build network permits HTTPS egress only. Call harbor_validate_candidate for structured diagnostics; submit no candidate bytes in prose. Work efficiently: web search and general internet access are unavailable and must never be attempted. Do not perform open-ended repository exploration; make only the targeted reads needed to write the candidate, then write every candidate file and call harbor_validate_candidate within this same turn.","Validator runtime uses --network none and a read-only root filesystem; /source and /task are read-only, and /work is a fresh writable copy of /source. solution/solve.sh and tests/test.sh must operate on /work through HARBOR_WORKSPACE, never write source/ or /source. For Rust/Cargo browser-wasm tasks, install generic wasm/browser tools in environment/Dockerfile, rely only on pinned base-image, repository-vendored, or frozen-input dependencies at runtime, and set CARGO_HOME=/work/.cargo-home and CARGO_TARGET_DIR=/work/.cargo-target instead of copying full Cargo registry/target caches into limited /tmp.","Use the same fenced workspace to correct only diagnostics bound to the candidate and frozen VerificationContract. When that contract invokes test.sh through sh, both test.sh and solution/solve.sh must be POSIX sh compatible: do not use set -o pipefail, bash arrays, [[, or other Bash-only syntax. Call harbor_validate_candidate with a passing candidate; the host captures the immutable snapshot."],"max_output_bytes":262144,"fingerprint":"sha256:722a59d6f48870874233e4865def4702fdeb03a6fb519a183ff375852b2326c3"}