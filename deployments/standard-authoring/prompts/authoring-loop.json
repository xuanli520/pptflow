{"id":"standard-authoring.v3.authoring-loop","version":"3.0.1","turn_prompts":["Read validation_environment_contract before writing files. In the authoring workspace, source/ is a read-only reference and task/ is the only writable candidate directory; write exactly instruction.md, task.toml, environment/Dockerfile, solution/solve.sh, tests/test.sh, and tests_analysis.json under task/. When provisioning Debian packages in environment/Dockerfile, switch the default Debian APT sources from HTTP to HTTPS before apt-get update; the controlled build network permits HTTPS egress only. Call harbor_validate_candidate for structured diagnostics; submit no candidate bytes in prose.","Validator runtime uses --network none and a read-only root filesystem; /source and /task are read-only, and /work is a fresh writable copy of /source. solution/solve.sh and tests/test.sh must operate on /work through HARBOR_WORKSPACE, never write source/ or /source. For Rust/Cargo browser-wasm tasks, install generic wasm/browser tools in environment/Dockerfile, rely only on pinned base-image, repository-vendored, or frozen-input dependencies at runtime, and set CARGO_HOME=/work/.cargo-home and CARGO_TARGET_DIR=/work/.cargo-target instead of copying full Cargo registry/target caches into limited /tmp.","Use the same fenced workspace to correct only diagnostics bound to the candidate and frozen VerificationContract. When that contract invokes test.sh through sh, both test.sh and solution/solve.sh must be POSIX sh compatible: do not use set -o pipefail, bash arrays, [[, or other Bash-only syntax. Call harbor_validate_candidate with a passing candidate; the host captures the immutable snapshot."],"max_output_bytes":262144,"fingerprint":"sha256:4f19ac85918c1239c9900712f3b4f3e87c18e7fd82f3efd89b25fd2e4b52894e"}
