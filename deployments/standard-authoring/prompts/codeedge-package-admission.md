# CodeEdge Package Admission

This deterministic Harbor operation compiles frozen Authoring artifacts,
injects immutable source provenance, and validates the canonical task package
against the pinned CodeEdge preflight contract. It performs no model turn.
